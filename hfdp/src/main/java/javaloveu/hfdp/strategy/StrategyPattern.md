# *Strategy Pattern*
```
*Strategy Pattern* defines a family of algorithms, encapsulates each one,
and makes them interchangerable.
Strategy lets the algotithm vary independently from clients that use it.
```
